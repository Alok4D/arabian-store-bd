# The connection string to your PostgreSQL database
DATABASE_URL="postgresql://username:password@localhost:5432/arabianstore?schema=public"

# The port where your Express API will run
PORT=5000

# To allow the frontend to safely communicate with the backend
CORS_ORIGIN="http://localhost:3000"
